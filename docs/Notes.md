Notes

To use debugger: open VS Code, set breakpoint on the pass line in _oob_check, run via "Python Debugger: Debug File", then trigger the condition

git tagging for releases:

git tag v1.0.1
git push origin v1.0.1

pip install pre-commit  #enables local pre-commit hooks


# running the game
py -m src 